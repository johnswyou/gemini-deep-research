# Project: gdr — Gemini Deep Research CLI

Python 3.10+ CLI wrapping Google's Deep Research / Deep Research Max via
the Gemini Interactions API. Binary name: `gdr`. Package: `gemini-deep-research`.

**Authoritative plan:** `/Users/john.you/.claude/plans/sunny-hopping-crown.md`
(10 phases total; phases 1-9 complete, phase 10 next).

---

## Codebase Patterns

Read this section BEFORE editing. Rules discovered the hard way — don't
re-learn them.

### Tooling / lint
- **Ruff bugbear B008:** Typer's `typer.Option(...)` / `typer.Argument(...)`
  in default values trip B008. Fixed via
  `[tool.ruff.lint.flake8-bugbear] extend-immutable-calls = ["typer.Option", "typer.Argument"]`
  in `pyproject.toml`. DO NOT add `# noqa: B008` — use this config.
- **Ruff PLC0415 (import not at top):** only two exceptions are allowed
  project-wide. (a) `from google import genai` is lazy-imported inside
  `GdrClient.__init__` so `gdr --help` / `--version` stay fast. Use
  `# noqa: PLC0415` with a comment. (b) `src/gdr/cli.py` has a per-file
  ignore for PLC0415 (declared in `pyproject.toml`).
- **Ruff RUF001/RUF003 (ambiguous unicode):** en-dash `–` trips in strings
  and comments. Use ASCII hyphen `-`. Em-dash in docstrings is fine per
  existing code (they're not flagged).
- **Ruff RUF100 (unused noqa):** `A002` and `BLE001` are NOT in the lint
  set — don't add noqa for them.
- **Ruff PLR0915 (too many statements):** the 50-statement limit catches
  `run()` easily. Extract helpers like `_finalize_and_render`,
  `_consume_create_result`, `_safe_build_client`.
- **Ruff PLW2901 (for-loop var reassignment):** rename `stripped = line.strip()`
  to avoid reassigning loop var `line`.
- **Ruff RUF059 (unused unpack):** prefix unused tuple members with `_`:
  `agg, _events = _collect(...)`.
- **Ruff E402 (imports not at top):** don't put module-level assignments
  (like `_UTC = timezone.utc`) between imports. Put them after ALL imports.

### mypy (strict, target py310)
- `datetime.UTC` is 3.11+. Use `from datetime import timezone` and
  `timezone.utc` (aliased as `_UTC = timezone.utc` at module scope, after
  imports). This is project-wide.
- `tomli` import has a mypy override (only imported on 3.10 branch) in
  pyproject.
- google.genai has a mypy override (ignore_missing_imports).
- Function naming collision: `Store.list` shadows the builtin `list` in
  class scope, breaking `list[Record]` annotations. This class uses
  `Store.recent(...)` instead. DO NOT rename back.
- Cast `policy.redact(raw)` to `dict[str, Any]` — the redactor returns
  `Any` generically.

### Domain architecture
- **`execute_research()` is the shared execution pipeline.** Both
  `gdr research` (with/without `--plan`) and `gdr plan approve` call it.
  Do NOT duplicate create/poll/render logic. The function accepts
  `previous_interaction_id` and `api_input` for plan-approval cases.
  As of Phase 6 it also accepts derived-domain kwargs
  (`builtin_tools`, `mcp_servers`, `file_search`, `input_parts`,
  `visualization`, `untrusted_input`). The CLI layer parses flags via
  `core/inputs.py` into these types — keep parsing out of
  `execute_research`.
- **`core/inputs.py` is where CLI flags become domain objects.** Every
  parser raises `ConfigError` (never bare `ValueError`), so the CLI's
  try/except → exit code 4 is uniform. `commands/research.py` stays lean
  by delegating all flag parsing here.
- **Auto-untrusted mode fires from `--file` or `--url`** when
  `config.safe_untrusted=True` (the default). `--untrusted-input` also
  trips it manually. The resulting `SecurityPolicy(untrusted=True)` is
  the single place tools get filtered (`code_execution`, `mcp_server`).
  `file_search` is considered safe and stays.
- **Tool ordering in the wire shape is contractual:** builtin simple
  tools → `file_search` → `mcp_server`. Tests assert this explicitly
  (`TestCombinedToolsAndMultimodal`). Reordering changes how dry-runs
  and fixtures look downstream.
- **--mcp-header splits on FIRST `:`** so values like `Bearer abc:123`
  survive. `str.partition("=")` then `str.partition(":")` on the rest.
- **--file-search-store accepts bare names**; the CLI prepends
  `fileSearchStores/`. `FileSearchSpec` still enforces the prefix, so
  config-TOML users get the same guarantee.
- **Auto-append `url_context` tool** when `--url` is used *and* the user
  overrode `--tool`. Handled in `_parse_flag_inputs` via
  `ensure_url_context_tool`. Config defaults already include
  `url_context`, so the branch is only hit when the user narrows tools.
- **`commands/_common.py` is for Phase-7+ family helpers** (client
  build, record lookup, `parse_since`). Do NOT dump research-command
  helpers here — those stay in `commands/research.py`. Rule of thumb:
  if ≥2 command modules need it, factor it to `_common`; otherwise
  keep it local.
- **`parse_since` accepts three forms:** relative (`7d`, `24h`, `30m`,
  `2w`), date-only (`YYYY-MM-DD`), and full ISO 8601 (with `Z` or
  offset). Anything else raises `ConfigError`. The helper takes a `now`
  kwarg for deterministic tests.
- **`gdr resume` never overwrites artifacts by default.** On collision
  it writes to a sibling directory suffixed `_resumed_<ts>`. The user
  opts in to overwriting via `--force`. Record lookup is required — no
  local record means we can't reconstruct a `RunContext`, so the
  command exits 4 and suggests `gdr research`.
- **`gdr cancel` detects missing SDK method gracefully.** Older
  `google-genai` builds may not expose `interactions.cancel`. We check
  via `getattr(client.interactions, "cancel", None)` and print a clear
  "upgrade your SDK" message on absence (exit 4) rather than a raw
  AttributeError.
- **`gdr show` supports id prefix match.** If an exact id lookup misses,
  we try `store.recent()` filtered by prefix. Returns None on ambiguity
  so the user gets the "no record" error (avoids accidental wrong-run
  reads when prefixes collide).
- **`gdr config set` regenerates the TOML file** from the parsed dict.
  Comments are lost. Use `gdr config edit` as the escape hatch for
  comment-preserving or nested changes. This was a deliberate choice
  to avoid adding `tomli-w`/`tomlkit` as a dependency.
- **`gdr config set` validates via Pydantic before writing.** The new
  value goes into the existing dict, and `Config.model_validate` runs
  against the whole thing. If validation fails (unknown key, bad enum
  value, bad type) we refuse to touch the file and exit 4. Means
  users can't get the config into a half-broken state.
- **`gdr config path` uses `typer.echo` (plain stdout), not
  `console.print`.** Rich wraps long paths at terminal width; scripts
  like `path=$(gdr config path)` need the raw string. Same rule applies
  to any command whose output is primarily machine-readable.
- **`gdr doctor` probes network via DNS lookup, not an API call.**
  `socket.gethostbyname("generativelanguage.googleapis.com")` costs
  zero API quota and is the right proxy for "can I reach Google".
  Tests patch `gdr.commands.doctor.socket.gethostbyname` via an
  autouse fixture so the suite never actually hits DNS.
- **`gdr doctor --fix` is idempotent.** Re-running it never re-creates
  existing files/dirs. The test suite exercises this explicitly by
  running `--fix` twice back-to-back and asserting both exits are 0.
- **Attribute-then-key `_get` helper** appears in three modules
  (`rendering.py`, `streaming.py`, `planning.py`). It bridges SDK typed
  objects (attribute access) and dict-shaped test mocks (key access) so
  tests can pass `SimpleNamespace` OR `dict` without a separate adapter.
- **Stream contract:** `interaction.complete` during streaming has
  `outputs=None` per Google's docs. The research command ALWAYS calls
  `.get(id=...)` after the stream ends — whether cleanly or by
  disconnect — to get authoritative outputs. Partial buffers are
  discarded on disconnect. Never reconstruct reports from the stream.
- **MCP header validation is eager.** `build_tools()` validates headers
  BEFORE any wire-shape assembly. One bad header aborts the whole
  request so partial kwargs never reach the API.
- **Untrusted-mode tool filtering runs LAST** in `build_tools()` so the
  `stripped` return list reflects what would actually have been sent.
- **Collaborative planning kwargs are minimal.** `build_plan_kwargs()`
  sets `thinking_summaries="none"` and `visualization="off"` — plans
  should return fast; streaming UI adds no value.

### Rendering
- When printing streamed text, use `console.print(text, end="",
  highlight=False, markup=False, soft_wrap=True)`. Model output may
  contain `[bold]` brackets that would otherwise crash Rich's markup
  parser.
- Text delta buffer in `LiveRenderer` flushes on newline. Use
  `finish()` at end of stream to empty the tail buffer.
- Use Rich's `Status` (not `Live`) for polling spinners — `Live` owns
  the viewport and wipes streamed prints above it.
- **Image outputs are extracted defensively.** `extract_images()` skips
  entries with missing `data` or unparseable base64 so garbage on the
  wire never aborts the full render. The raw transcript still captures
  the original payload for debugging.
- **Image filenames are `image_NNN<.ext>`** (3-digit zero-padded),
  extension from `mimetypes.guess_extension(mime)` with `.png` fallback.
  stdlib may pick `.jpe` for `image/jpeg`; tests check only the prefix.
- **Images section goes *between* body and Sources** in `report.md` —
  tests assert "## Images" before "## Sources" ordering.

### Storage + paths
- Store: `$GDR_STATE_DIR` → `$XDG_STATE_HOME/gdr/` → `~/.local/state/gdr/`.
- Config: `$GDR_CONFIG_PATH` → `$XDG_CONFIG_HOME/gdr/config.toml` →
  `~/.config/gdr/config.toml`.
- Intentionally NOT using `platformdirs` — predictability over platform
  idiom for terminal tooling.
- `env:VAR` in TOML is **prefix-only** (`"env:TOKEN"` works;
  `"Bearer env:TOKEN"` does not). Tested + documented contract.
- Slug sanitization: `[^a-z0-9]+ → -`, max 64 chars, empty falls back to
  `"query"`. id6 fragment (first 6 alnum of interaction id) appended.
  Dashes in id strip out — test IDs in fixtures use dash-free strings
  (`"intabcxyz123"`) so `intabc` is predictable.

### Testing
- Fake the SDK at `google.genai.Client` via `mocker.patch`. Tests pass
  `SimpleNamespace` or `MagicMock` objects; the `_get` helpers accept
  both shapes. See `tests/unit/test_research_command.py::_install_fake_sdk`.
- Golden-file fixtures for streaming live in
  `tests/fixtures/streams/*.jsonl` (one JSON event per line). These
  cover happy path / mid-stream error / out-of-order / disconnect /
  interleaved content.
- Mock `typer.prompt` via `mocker.patch("gdr.core.planning.typer.prompt",
  side_effect=[...])` for interactive-loop tests.
- Rich output capture: `Console(file=io.StringIO(), record=True,
  force_terminal=False, width=120)` then `console.export_text()`.
- All command tests use an autouse `_isolated_env` fixture setting
  `GDR_STATE_DIR` and `GDR_CONFIG_PATH` to tmp_path so tests never
  touch real user state.

### Git / commit conventions
- Attribution is disabled globally per user's settings. Do NOT add
  `Co-Authored-By: Claude` — the commit message rule explicitly omits
  it. Commit body format: `<type>: <description>\n\n<body>`. Types used
  so far: `chore`, `feat`, `feat(core)`. One phase = one commit.

---

## Current state (as of Phase 9 complete)

### Commits
```
a59a600 docs: usage guide, MCP how-to, examples     (Phase 9)
70010ef docs: record Phase 8 commit hash in progress log
c60b2b0 feat: config and doctor commands            (Phase 8)
995e5d2 docs: record Phase 7 commit hash in progress log
15b7d1c feat: history, resume, follow-up, cancel          (Phase 7)
7268ad5 docs: record Phase 6 commit hash in progress log
bf81ec5 feat: tools, MCP, multimodal inputs, visualization (Phase 6)
6ee27e9 feat: collaborative planning flow                (Phase 5)
d4f128d feat: streaming with live thought summaries...    (Phase 4)
947f33d feat: research command (polling, markdown...)     (Phase 3)
08aa2bb feat(core): client wrapper, config loader...      (Phase 2)
288feba chore: scaffold project structure                 (Phase 1)
```

### Shipped surface
- `gdr research <query> [--max] [--plan] [--stream/--no-stream]
  [--output DIR] [--dry-run] [--api-key KEY] [--no-confirm]
  [--config PATH] [--tool NAME]... [--mcp NAME=URL]...
  [--mcp-header NAME=Key:Value]... [--file PATH]... [--url URL]...
  [--file-search-store NAME]... [--visualization auto|off]
  [--untrusted-input]`
- `gdr plan refine <id> <feedback>` — one-shot refinement
- `gdr plan approve <id> [--query LABEL] [--stream/--no-stream]
  [--output DIR] [--dry-run] [--api-key] [--config]`
- `gdr ls [--limit N] [--status S] [--since DATE] [--full-id] [--config]`
- `gdr show <id> [--part text|sources|metadata|transcript|images] [--config]`
- `gdr status <id> [--api-key] [--config]`
- `gdr resume <id> [--force] [--api-key] [--config]`
- `gdr follow-up <id> <question> [--max] [--stream/--no-stream]
  [--output] [--dry-run] [--api-key] [--no-confirm] [--config]`
- `gdr cancel <id> [--api-key] [--config]`
- `gdr config {path,get,set,edit} [--config PATH]`
- `gdr doctor [--fix] [--config PATH]`
- `gdr --version` / `gdr -V`

### Module map (src/gdr/)
- `cli.py` — Typer app root; registers research + plan + history commands.
- `commands/research.py` — `run()` Typer entry + `execute_research()`
  shared helper. **This is THE execution pipeline.**
- `commands/plan.py` — `refine_cmd`, `approve_cmd`; both delegate core
  logic to shared modules.
- `commands/_common.py` — **Shared helpers for the history/follow-up
  family.** `build_client`, `load_cfg`, `open_store`, `lookup_record`,
  `parse_since`, `get_attr_or_key`. Keep small — anything interesting
  belongs in `core/`.
- `commands/ls.py` — `gdr ls` (pure read from JsonlStore).
- `commands/show.py` — `gdr show <id> --part …` (reads artifacts from
  the run's output_dir). Supports prefix-match on id.
- `commands/status.py` — `gdr status <id>` (one-shot .get, no polling).
- `commands/resume.py` — `gdr resume <id> [--force]`; polls to
  completion then rewrites artifacts.
- `commands/follow_up.py` — `gdr follow-up <id> <question>`; delegates
  to `execute_research` with `previous_interaction_id=<id>`.
- `commands/cancel.py` — `gdr cancel <id>`; short-circuits on terminal
  status, detects missing `interactions.cancel` method gracefully.
- `commands/config.py` — `gdr config` Typer sub-app with `path/get/set/edit`.
  Pure-Python TOML writer (no `tomli-w` dep) covering the Config shape.
  Comment preservation = not supported; `gdr config edit` is the escape
  hatch for complex/nested changes.
- `commands/doctor.py` — `gdr doctor [--fix]`. Runs 7 checks (Python,
  google-genai, config file, API key, network DNS, output_dir,
  state_dir) and prints a Rich table. `--fix` creates missing dirs +
  config template. Exit 0 on warn-only, 4 on any failure.
- `core/client.py` — `GdrClient` SDK façade (lazy genai import, key
  fingerprint, SDK version check for interactions API).
- `core/models.py` — Frozen Pydantic: `RunContext`, `AgentConfig`,
  `TextPart`/`MediaPart` (discriminated `InputPart`), `McpSpec`,
  `FileSearchSpec`, `Record`.
- `core/inputs.py` — **CLI flag → domain object parsers.** `parse_file`,
  `parse_mcps`, `parse_file_search_stores`, `urls_as_text_part`,
  `ensure_url_context_tool`, `validate_tool_names`,
  `validate_visualization`. All raise `ConfigError` on bad input.
- `config.py` — TOML loader with env expansion, Pydantic-validated.
- `core/requests.py` — `build_create_kwargs(ctx, policy) → (kwargs,
  stripped)`. Single choke point for RunContext → SDK dict.
- `core/rendering.py` — `write_artifacts()` emits `report.md`,
  `sources.json`, `metadata.json`, `transcript.json` (redacted),
  plus `images/image_NNN<.ext>` when outputs contain image payloads.
  `extract_images()` + `write_images()` are public helpers.
- `core/persistence.py` — `Store` protocol + `JsonlStore` (append-only
  JSONL, in-memory index, `Store.recent()` for chronological query,
  `Store.list_children()` for parent-id tree).
- `core/security.py` — `SecurityPolicy` with MCP header validation,
  path confinement, redaction, untrusted-input tool filtering.
- `core/streaming.py` — `StreamAggregator` + `StreamEvent` for SSE
  consumption.
- `core/planning.py` — `PlanRequest`, `PlanDecision`,
  `interactive_plan_loop()`.
- `ui/progress.py` — `poll_until_complete()` + `run_with_live_status()`.
- `ui/live.py` — `LiveRenderer` + `stream_with_live_ui()`.
- `constants.py`, `errors.py` — shared constants and typed exceptions
  mapped to exit codes (0/1/2/3/4/5/130).

### Test coverage (348 tests, ruff/mypy strict clean)
- `test_cancel_command.py` (4) — Phase 7. In-progress cancel,
  already-terminal short-circuit, SDK missing cancel, API failure.
- `test_cli.py` (4) — `--version`, `-V`, `--help`, no-args.
- `test_client.py` (8) — GdrClient construction + fingerprint.
- `test_commands_common.py` (12) — Phase 7. `parse_since` relative
  + absolute + error cases.
- `test_config_command.py` (16) — **Phase 8.** path/get/set/edit
  subcommands. Includes nested-table preservation test, nested-key
  rejection, invalid-value rejection, editor mock tests.
- `test_doctor_command.py` (8) — **Phase 8.** All-pass happy path,
  missing API key (fail), DNS failure (fail), missing config (warn),
  --fix creates dirs + template, --fix idempotency, malformed config,
  non-writable output_dir.
- `test_config.py` (20) — TOML parsing, env expansion, defaults.
- `test_follow_up_command.py` (3) — **Phase 7.** Passes
  previous_interaction_id, dry-run, writes artifacts.
- `test_inputs.py` (41) — Phase 6 flag parsing. --file/--url/--mcp/
  --mcp-header/--file-search-store/--visualization/--tool.
- `test_live.py` (12) — `LiveRenderer` + `stream_with_live_ui`.
- `test_ls_command.py` (7) — **Phase 7.** Empty store, ordering,
  --limit, --status, --since, --full-id.
- `test_models.py` (16) — Pydantic validation and frozen-ness.
- `test_persistence.py` (16) — JsonlStore CRUD + paths.
- `test_planning.py` (20) — helpers + `interactive_plan_loop`.
- `test_plan_command.py` (9) — `gdr plan refine|approve`.
- `test_progress.py` (11) — `poll_until_complete` with fake clock/sleep.
- `test_rendering.py` (26) — report/sources/metadata/transcript, plus
  Phase 6 image extraction + write_images + report ordering.
- `test_requests.py` (19) — kwargs assembly, MCP validation, plus
  Phase 6 combined tools + multimodal cases.
- `test_research_command.py` (27) — dry-run, happy path, max confirm,
  output override, streaming, disconnect, --plan approve/refine/cancel,
  Phase 6 CLI-level flows.
- `test_resume_command.py` (4) — **Phase 7.** Terminal resume, missing
  record, suffix-on-collision, --force overwrite.
- `test_security.py` (32) — header validation, slug, path, redaction.
- `test_show_command.py` (9) — **Phase 7.** Each --part, unknown id,
  prefix match, missing output_dir.
- `test_status_command.py` (4) — **Phase 7.** Completed, in_progress
  with thought, elapsed time, API failure.
- `test_streaming.py` (19) — golden fixtures for all 5 event scenarios.

### Gate commands
```
uv run ruff check .
uv run ruff format --check .
uv run mypy src
uv run pytest -q
uv run pytest --cov=src/gdr --cov-report=term-missing
```

### Smoke checks (run without API key)
```
uv run gdr research "smoke" --dry-run --api-key AIza-xxxx-xxxx
uv run gdr research "smoke" --plan --dry-run --api-key AIza-xxxx-xxxx
uv run gdr plan approve "plan-id-123" --dry-run
uv run gdr research "smoke" --dry-run --api-key AIza-xxxx-xxxx \
    --tool google_search --mcp deploys=https://mcp.example.com \
    --mcp-header 'deploys=Authorization:Bearer xyz' \
    --file-search-store kb-2025 --visualization off
```

---

## Phase 10 (REMAINING — deferred by user, log preserved for future session)

**Scope per plan:**

> CI + release — GitHub Actions: lint (Ruff) + typecheck (Mypy) +
> test (Pytest on 3.10/3.11/3.12/3.13) on PR; publish to PyPI on git
> tag `v*` via OIDC trusted publishing.
>
> **Plan commit message:** `ci: GitHub Actions lint/test/release`
> (splits are fine: one commit for CI, one for release, one for docs
> if CHANGELOG lands).

### Pre-flight state (as of end of Phase 9 on 2026-04-22)

Quick facts a future session should verify before starting:

| Fact | Value |
| --- | --- |
| Current version | `0.1.0` (in BOTH `pyproject.toml` AND `src/gdr/__init__.py`) |
| Package name on PyPI | `gemini-deep-research` (unreserved as of Phase 9) |
| Binary | `gdr` (script entry in `pyproject.toml::[project.scripts]`) |
| `.github/` directory | Does NOT exist yet — create it |
| `uv.lock` | Exists at repo root — use `uv sync --frozen` in CI |
| Test count | 348 (baseline for the CI matrix to hit) |
| Python target | `requires-python = ">=3.10"` |
| Build backend | `hatchling` (already configured; produces sdist + wheel cleanly) |

Run `cd /Users/john.you/personal/projects/gemini-deep-research-agent
&& uv run pytest -q && uv run ruff check . && uv run mypy src` first
to confirm the baseline is still green before touching CI.

### Implementation checklist

#### 1. `.github/workflows/ci.yml`

Create the file. Starting skeleton (works with current `uv.lock`):

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}
      - name: Sync deps (frozen to uv.lock)
        run: uv sync --frozen --extra dev
      - name: Ruff lint
        run: uv run ruff check .
      - name: Ruff format check
        run: uv run ruff format --check .
      - name: Mypy (strict)
        run: uv run mypy src
      - name: Pytest with coverage
        run: uv run pytest -q --cov=src/gdr --cov-report=term-missing
```

Notes:
- `astral-sh/setup-uv@v3` handles uv install + cache. Don't use
  `actions/setup-python` separately — `uv python install` after that
  is the more idiomatic flow and avoids Homebrew shadowing on macOS.
- 3.13 is listed; if CI fails on `google-genai` compatibility, drop
  it from the matrix (keep 3.10-3.12). Verify locally first:
  `uv run -p 3.13 pytest -q`.
- `--frozen` requires `uv.lock` to be current. If `pyproject.toml`
  ever gets new deps added, run `uv lock` locally and commit the
  update in the same PR or CI will fail with a lock mismatch.

#### 2. `.github/workflows/release.yml`

Create the file. Skeleton (publishes on `v*` tags, skips `-rc` tags):

```yaml
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write   # create GitHub Release
  id-token: write   # required for PyPI OIDC trusted publishing

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - name: Build sdist and wheel
        run: uv build

      # Upload artifacts to the GitHub Release for both RC and
      # stable tags; gives auditors a paper trail.
      - name: Create GitHub Release with artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          draft: false
          prerelease: ${{ contains(github.ref, 'rc') }}

      # PyPI publish ONLY for stable tags (v1.2.3), never for RCs.
      # Requires trusted publisher config on PyPI — see notes below.
      - name: Publish to PyPI
        if: ${{ !contains(github.ref, 'rc') }}
        uses: pypa/gh-action-pypi-publish@release/v1
```

The `if: !contains(github.ref, 'rc')` gate is the thing that lets you
do `v0.1.0-rc.1` as a safe dry-run. RC tags build artifacts and make
a prerelease GitHub Release but do NOT touch PyPI.

#### 3. PyPI trusted publisher — one-time setup

Before pushing any tag, configure PyPI to trust this GitHub repo:

1. Go to https://pypi.org/manage/account/publishing/
   (if `gemini-deep-research` doesn't exist yet, use the "pending
   publisher" flow at the top of that page — it creates the project
   on first successful publish).
2. Add a publisher with:
   - PyPI project name: `gemini-deep-research`
   - Owner: `gdr-cli` (or whatever org ends up owning the repo)
   - Repository name: `gemini-deep-research`
   - Workflow filename: `release.yml`
   - Environment (optional but recommended): `pypi`
3. If using an environment, add `environment: pypi` under `jobs.
   build-and-publish` in the release workflow.

No API token goes in GitHub secrets — OIDC is the whole point.

#### 4. `CHANGELOG.md` (recommended)

Use Keep A Changelog format. Starter content:

```markdown
# Changelog

All notable changes to this project will be documented in this file.
Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [0.1.0] - 2026-04-22

First public release. Ships the full CLI surface.

### Added
- `gdr research <query>` with polling, streaming with live thought
  summaries, collaborative planning (`--plan`), and auto-recovery
  from stream disconnects.
- Tool + multimodal input flags: `--tool`, `--mcp NAME=URL`,
  `--mcp-header NAME=K:V`, `--file PATH`, `--url URL`,
  `--file-search-store NAME`, `--visualization auto|off`,
  `--untrusted-input`.
- History commands: `gdr ls`, `gdr show`, `gdr status`,
  `gdr resume`, `gdr follow-up`, `gdr cancel`.
- Operator commands: `gdr config {path,get,set,edit}` and
  `gdr doctor [--fix]`.
- XDG-style local state and config (`$XDG_STATE_HOME/gdr/`,
  `$XDG_CONFIG_HOME/gdr/config.toml`), env-var overrides
  (`GDR_STATE_DIR`, `GDR_CONFIG_PATH`).
- Security: MCP header validation, redaction in `transcript.json`,
  path confinement under `output_dir`, untrusted-input tool
  filtering.
- Artifact layout: `report.md`, `sources.json`, `metadata.json`,
  `transcript.json`, `images/image_NNN.<ext>`.
- 348 unit tests, 93% line coverage, ruff + mypy strict clean.
```

#### 5. Dry-run + first release

After the workflows land and PyPI trusted publisher is configured:

```bash
# Sanity pass (no real work yet)
cd /Users/john.you/personal/projects/gemini-deep-research-agent
uv run pytest -q && uv run ruff check . && uv run mypy src

# Confirm versions match
grep '^version' pyproject.toml
grep '__version__' src/gdr/__init__.py

# Local build smoke — produces dist/*.whl and dist/*.tar.gz
uv build
ls -lh dist/

# RC dry-run: push a tag, watch the release workflow, verify
# artifacts show up on the GitHub Release page but NOT on PyPI.
git tag v0.1.0-rc.1
git push origin v0.1.0-rc.1

# If the RC release workflow succeeds and the GitHub Release page
# has both .whl and .tar.gz, promote:
git tag v0.1.0
git push origin v0.1.0

# Final verification on a clean machine (or in a fresh venv):
pipx install gemini-deep-research
gdr --version    # should print "gdr 0.1.0"
gdr doctor       # should pass most checks
```

### Files touched in Phase 10

Expected diff (use as a checklist):

- `.github/workflows/ci.yml`         — NEW
- `.github/workflows/release.yml`    — NEW
- `CHANGELOG.md`                     — NEW (optional)
- `progress.txt`                     — add Phase 10 progress log entry
- Possibly `pyproject.toml`          — only if we need to bump a
  dep version or add a missing classifier; not otherwise.

Do NOT touch `src/gdr/**` during Phase 10. This phase is
infrastructure-only; any code change that sneaks in masks what CI is
actually validating.

### Gotchas to watch for in Phase 10

- **OIDC permissions block.** The release workflow needs both
  `id-token: write` (for PyPI OIDC) AND `contents: write` (for
  GitHub Release creation). Missing either fails with a cryptic
  error like `Resource not accessible by integration`.
- **`uv.lock` must be committed and current.** CI uses
  `uv sync --frozen` so any dep drift fails the build. If a PR
  touches `pyproject.toml`, run `uv lock` in that PR too.
- **Python 3.13 may fail on `google-genai`.** The SDK's minimum
  supported Python shifts over time. If 3.13 goes red in CI but
  3.10-3.12 are green, remove 3.13 from the matrix rather than
  pin-waiting on an SDK release.
- **macOS runners have Homebrew Python.** `actions/setup-python`
  can collide with it silently. Using `astral-sh/setup-uv@v3`
  followed by `uv python install <version>` avoids this — that's
  why the skeleton above is shaped that way. Do NOT use
  `actions/setup-python@v5` in parallel with `uv`.
- **PyPI project name already taken.** Before configuring the
  trusted publisher, check
  https://pypi.org/project/gemini-deep-research/ — if it's taken
  (someone squatted it between now and when you ship), either
  pick a new name (update `pyproject.toml::[project].name`, the
  README install instructions, and the docs) or go through PyPI's
  package-squatting-dispute flow.
- **Pre-release tags must NOT publish to PyPI.** The release
  workflow above gates on `!contains(github.ref, 'rc')`. If you
  ever rename RC tags to something else (`-beta`, `-dev`), update
  that check — otherwise the next beta tag will ship to PyPI.
- **Version drift between `pyproject.toml` and `__init__.py`.**
  Both must match. Before tagging, grep both:
  ```bash
  grep '^version' pyproject.toml
  grep '__version__' src/gdr/__init__.py
  ```
  If they differ, the tag will produce a wheel whose metadata and
  `gdr --version` output disagree. A sync script or a dynamic
  version via `hatchling.version` is a v1.1 improvement.
- **No secret tokens in `dist/*.tar.gz`.** Hatch respects
  `.gitignore` so `progress.txt` and `CLAUDE.md` land in the sdist
  as-is, which is fine (they contain no secrets). But confirm with
  `tar tzf dist/gemini_deep_research-0.1.0.tar.gz | less` before
  the first real PyPI upload.
- **GitHub Actions cache key evolution.** `astral-sh/setup-uv@v3`'s
  `enable-cache: true` keys on `uv.lock` by default. If you
  downgrade to `@v2` the key semantics differ — re-test before
  merging a version bump to the action itself.

### Success criteria for Phase 10

Phase 10 is "done" when ALL of these are true:

- [ ] `.github/workflows/ci.yml` exists, is green on PR, covers 3.10-3.13
      on Linux and macOS, and runs ruff / ruff-format / mypy / pytest.
- [ ] `.github/workflows/release.yml` exists with OIDC trusted-publisher
      config, `id-token: write` + `contents: write` permissions, and
      the RC-tag gate.
- [ ] PyPI trusted publisher is configured for the
      `gemini-deep-research` project.
- [ ] `v0.1.0-rc.1` tag pushed; GitHub Release shows the artifacts;
      PyPI was NOT touched.
- [ ] `v0.1.0` tag pushed; PyPI shows `gemini-deep-research 0.1.0`;
      `pipx install gemini-deep-research` on a fresh machine produces
      a working `gdr` binary.
- [ ] `CHANGELOG.md` exists (optional but strongly recommended).
- [ ] `progress.txt` has a Phase 10 progress-log entry with commit
      hashes and learnings.

### What happens after Phase 10

The project is feature-complete per the v1.0 plan. Items explicitly
**deferred to v1.1 or later** by the original plan:

- `--cost-estimate` flag (no pre-run pricing signal exists).
- `html`/`pdf` export formats (downstream tooling on `report.md`
  handles this well enough).
- `gdr init` wizard (merged into `doctor --fix`).
- SQLite store migration (`Store` protocol already factored; can
  drop in without caller changes).
- Custom shell-completion (Typer `--install-completion` covers it).
- CodeQL / Dependabot security automation (GitHub defaults usually
  cover; add only if repo policy requires it).
- Dynamic version via `hatchling.version` (bigger refactor — OK to
  leave as manual sync for v1.0).

---

## Progress Log

### 2026-04-22 — Phases 1-5 complete (prior session)
- Shipped scaffold, core foundations, research MVP (polling), streaming
  with live UI, collaborative planning flow.
- 212 tests, 92% coverage, ruff/mypy strict clean.
- **Learnings for future iterations:**
  - All key patterns captured in "Codebase Patterns" section above -
    read that BEFORE editing.
  - The `execute_research()` shared helper is the single execution
    pipeline. Resist duplicating it.
  - Golden-file fixtures under `tests/fixtures/streams/` are the
    regression signal for the SDK event shape.
  - `_get` attribute-then-key helper is the core interop trick for
    tests <-> SDK objects.

### 2026-04-22 — Phase 6 complete (tools, MCP, multimodal, visualization)
- Shipped `--tool`, `--mcp`, `--mcp-header`, `--file`, `--url`,
  `--file-search-store`, `--visualization`, `--untrusted-input` flags.
- Added image rendering: base64 outputs decoded to
  `<run>/images/image_NNN<.ext>` and linked in `report.md` under a
  `## Images` section positioned between body and sources.
- New module `src/gdr/core/inputs.py` (214 lines) — all CLI flag
  parsers live here; every parser raises `ConfigError` so the CLI's
  existing exit-code-4 path covers everything uniformly.
- 280 tests pass (+68 from Phase 5), 93% coverage, ruff/mypy clean.

### 2026-04-22 — Phase 7 complete (history, resume, follow-up, cancel)
- Shipped six new commands: `gdr ls`, `gdr show`, `gdr status`,
  `gdr resume`, `gdr follow-up`, `gdr cancel`. All registered as
  top-level Typer commands in `cli.py`.
- New shared helper module `src/gdr/commands/_common.py` with:
  * `build_client`, `load_cfg`, `open_store`, `lookup_record`
  * `parse_since` — handles relative (7d/24h/30m/2w), date-only, and
    ISO 8601 formats. `ConfigError` on anything else.
  * `get_attr_or_key` — the attribute-then-key helper, but made
    public so command modules don't reach into private names.
- `gdr ls` uses `Store.recent()` with filters; Rich table output;
  shortens ids to 12 chars by default (`--full-id` to see everything).
- `gdr show` renders each --part with Rich; supports id prefix match.
  Exit 4 on unknown id or missing output_dir.
- `gdr status` is a one-shot `.get()` with colored status, elapsed
  time (when local record found), token count, and last thought.
- `gdr resume` reconstructs `RunContext` from the record, polls to
  completion if still in-progress, and writes artifacts. Collision
  handling: writes to `<original>_resumed_<ts>` sibling dir unless
  `--force` is passed.
- `gdr follow-up` is ~50 lines — thin wrapper around
  `execute_research(previous_interaction_id=<id>, api_input=<question>)`.
- `gdr cancel` checks current status first (short-circuits on
  terminal) and detects missing SDK method via `getattr` guard.
- 324 tests pass (+44 from Phase 6), ruff/mypy strict clean.
- Files added:
  - `src/gdr/commands/_common.py` (new)
  - `src/gdr/commands/ls.py` (new)
  - `src/gdr/commands/show.py` (new)
  - `src/gdr/commands/status.py` (new)
  - `src/gdr/commands/resume.py` (new)
  - `src/gdr/commands/follow_up.py` (new)
  - `src/gdr/commands/cancel.py` (new)
  - `tests/unit/test_commands_common.py` (new, 12 tests)
  - `tests/unit/test_ls_command.py` (new, 7 tests)
  - `tests/unit/test_show_command.py` (new, 9 tests)
  - `tests/unit/test_status_command.py` (new, 4 tests)
  - `tests/unit/test_resume_command.py` (new, 4 tests)
  - `tests/unit/test_follow_up_command.py` (new, 3 tests)
  - `tests/unit/test_cancel_command.py` (new, 4 tests)
- Files modified:
  - `src/gdr/cli.py` — register 6 new commands
- **Learnings for future iterations:**
  - Typer accepts hyphenated command names via `app.command("follow-up")`;
    the underlying function lives in `follow_up.py` (Python naming).
  - `Enum` subclassing `str, Enum` works seamlessly with Typer as a
    `--part` choice; values are auto-documented in `--help`.
  - Tests that need a pre-seeded `JsonlStore` should write via the
    `JsonlStore.open(path)` API (not hand-craft the file) to avoid
    subtle format drift — the `Record.model_dump_json` shape is
    authoritative.
  - When typing Store protocol methods accept `protocol-only` calls,
    use the real `Store` import instead of `object` — mypy catches the
    difference and picks up return-type narrowing.
  - `mimetypes.guess_extension("image/jpeg")` returns `.jpe` on stdlib;
    tests covering filenames should match on prefix, not exact ext.
  - `ruff format` often reformats line-wrapping decisions on new files
    — run format AFTER writing, then the lint/mypy/test gate, to avoid
    noise in the final pass.

### 2026-04-22 — Phase 8 complete (config and doctor commands)
- Shipped `gdr config {path,get,set,edit}` and `gdr doctor [--fix]`.
- `gdr config` sub-app lives in `commands/config.py` with a hand-rolled
  TOML writer targeted at the Config shape (no `tomli-w` dep). Trade
  comment preservation for zero deps; `gdr config edit` is the escape
  hatch for users who want to keep comments.
- `gdr config set` validates via `Config.model_validate` before writing
  so invalid configs never hit disk — tested with the `thinking_summaries=maybe`
  case.
- `gdr doctor` runs 7 checks: Python, google-genai, config file, API
  key, network DNS, output_dir, state_dir. Exits 4 on any fail; 0 on
  warn-only or all-pass. `--fix` creates missing dirs and a config
  template; idempotent (tested twice in a row).
- 348 tests pass (+24 from Phase 7). ruff + mypy strict clean.
- Files added:
  - `src/gdr/commands/config.py` (new)
  - `src/gdr/commands/doctor.py` (new)
  - `tests/unit/test_config_command.py` (new, 16 tests)
  - `tests/unit/test_doctor_command.py` (new, 8 tests)
- Files modified:
  - `src/gdr/cli.py` — register config sub-app and doctor command
- **Learnings for future iterations:**
  - Rich's `console.print` wraps long strings at terminal width. For
    machine-readable single-line output (paths, ids), use
    `typer.echo(...)` instead. The failing test was `config path` with
    a long tmp_path — the path got split across two lines and the
    `in result.output` assertion failed.
  - Autouse DNS mock (`mocker.patch("...socket.gethostbyname")`) keeps
    `test_doctor_command.py` from flaking on network-less CI. Override
    the mock in the specific test that needs DNS to fail.
  - `subprocess.run` linting: Ruff's S603/S607 warn by default on list
    invocations. Keep the list form (no `shell=True`) and add `#
    noqa: S603` with a short "list form; no shell" justification.
  - `Config.model_validate` happily accepts defaults when the input
    dict is empty — useful for `gdr config get` on a missing file, but
    surprising if you expect an error. Test that behavior explicitly.
  - `doctor`'s `--fix` uses `mkdir(parents=True, exist_ok=True)` for
    idempotence. Tests that assert fix-twice-doesn't-crash catch the
    regression early.

### 2026-04-22 — Phase 9 complete (docs + examples)
- Shipped `docs/USAGE.md`, `docs/MCP.md`, `docs/TROUBLESHOOTING.md`
  and five runnable `examples/*.sh` scripts. README polished with
  accurate command table, fixed broken doc links (was pointing at
  SAFETY.md and PLAN.md which don't exist), and expanded config
  example.
- No code changes — 348 tests still pass unchanged.
- Docs content:
  - `USAGE.md` — every command with flags, exit-code table,
    artifact layout, config reference, env-var list.
  - `MCP.md` — MCP flag anatomy, header validation rules,
    redaction behavior, untrusted-input interaction, wire-shape
    ordering, troubleshooting.
  - `TROUBLESHOOTING.md` — exit-code-indexed fix recipes for each
    common failure mode.
  - `examples/01-05_*.sh` — basic / planning / MCP / multimodal /
    follow-up. All are `chmod +x` and use `set -euo pipefail`.
- Files added:
  - `docs/USAGE.md`
  - `docs/MCP.md`
  - `docs/TROUBLESHOOTING.md`
  - `examples/01_basic.sh`
  - `examples/02_planning.sh`
  - `examples/03_mcp.sh`
  - `examples/04_multimodal.sh`
  - `examples/05_follow_up.sh`
- Files modified:
  - `README.md` — fixed SAFETY.md / PLAN.md references,
    added `config path`, added `confirm_max` / `safe_untrusted`,
    added "Further reading" link block.
- **Learnings for future iterations:**
  - README had stale links to `docs/SAFETY.md` and `docs/PLAN.md`
    from the early scaffold. Grep for `docs/` references whenever
    adding or renaming docs to catch dangling pointers.
  - `USAGE.md`'s exit-code table is currently hand-maintained — if
    `errors.py` grows a new exception, the table drifts. A test that
    greps USAGE.md for `| N |` lines vs. `errors.py` exit_code
    attributes would be worth adding in v1.1.
  - Shell examples should default to non-interactive flags
    (`--no-confirm`) so they're copy-paste-safe from CI or a blog
    post. Keep interactive flags documented but off by default.
  - Don't document `--plan` inside follow-up examples — the flag
    doesn't exist on follow-up (planning happens once, at the
    parent). Tests enforce this implicitly via the follow-up
    command's signature.
  - Troubleshooting docs are most useful when indexed by the actual
    error message the user sees. Write them as "if you got THIS
    message, do THIS", not as abstract categories.
---
